# Assembler comparison experiment config.

# shellcheck source=/dev/null
source "/storage/biology/projects/miller-lowry/beitner/binning-classification-wrapper/scripts/configs/binning_base.conf"

EXPERIMENT_NAME="assembler_comparison_experiment"
BINNER="metawrap"

ASSEMBLIES=(idbaud megahit metaspades flye metamdbg metaspades_hybrid)
TARGET_SAMPLES=(S1 S2 S5)

SET_SUFFIX="assembler_compare"
COBINNING_MODE=0

OUTPUT_ROOT="${DATA_DIR}/binning_outputs/assembler_comparison_experiment"
