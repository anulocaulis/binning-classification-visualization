# Gold standard MAG library experiment config.

# shellcheck source=/dev/null
source "/storage/biology/projects/miller-lowry/beitner/binning-classification-wrapper/scripts/configs/binning_base.conf"

EXPERIMENT_NAME="gold_standard_mag_library"
BINNER="metawrap"

ASSEMBLIES=(idbaud megahit metaspades flye metamdbg metaspades_hybrid)
TARGET_SAMPLES=(S1 S2 S5)

# Gold-standard style pooled reads across selected read samples.
SET_SUFFIX="gold_standard"
COBINNING_MODE=1
READ_SAMPLES=(S1 S2 S5)

OUTPUT_ROOT="${DATA_DIR}/binning_outputs/gold_standard_mag_library"
